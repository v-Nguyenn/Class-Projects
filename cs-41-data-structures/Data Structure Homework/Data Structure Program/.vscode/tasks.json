{
   "tasks": [
      {
         "type": "cppbuild",
         "label": "C/C++: g++.exe build active file",
         // specifies the program to run, this case g++
         "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
         // args array specifies the command-line arguments 
         "args": [
            "-fdiagnostics-color=always",
            "-g",
            "${file}", // this is the active file
            "-o", // this is the output 
            // current directory {fileDirname}
            "${fileDirname}\\${fileBasenameNoExtension}.exe"
         ],
         "options": {
            "cwd": "${fileDirname}"
         },
         "problemMatcher": [
            "$gcc"
         ],
         "group": {
            "kind": "build",
            "isDefault": true
         },
         "detail": "Task generated by Debugger."
      }
   ],
   "version": "2.0.0"
}